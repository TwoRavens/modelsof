replace district = "ANANTPUR" if district=="ANANTAPUR" & FinalState=="ANDHRA PRADESH"
replace district = "CHITTOOR" if district=="CHITTOR" & FinalState=="ANDHRA PRADESH"
replace district = "CUDDAPAH" if district=="KADAPA" & FinalState=="ANDHRA PRADESH"
replace district = "EAST GODAVARI" if district=="AMALAPURAM" & FinalState=="ANDHRA PRADESH"
replace district = "KRISHNA" if district=="VIJAYAWADA" & FinalState=="ANDHRA PRADESH"
replace district = "PRAKASAM" if district=="BAPATLA" & FinalState=="ANDHRA PRADESH"
replace district = "RANGAREDDY" if district=="RANGAREDDI" & FinalState=="ANDHRA PRADESH"
replace district = "VISHAKHAPATNAM" if district=="VISAKHAPATNAM" & FinalState=="ANDHRA PRADESH"
replace district = "WEST GODAWARI" if district=="WEST GODAVARI" & FinalState=="ANDHRA PRADESH"
replace district = "BHOJPUR" if district=="ARRAH" & FinalState=="BIHAR"
replace district = "EAST CHAMPARAN" if district=="PURVI CHAMPARAN" & FinalState=="BIHAR"
replace district = "JEHANABAD" if district=="JAHANABAD" & FinalState=="BIHAR"
replace district = "PATNA" if district=="PATNA SAHIB" & FinalState=="BIHAR"
replace district = "PURNEA" if district=="PURNIA" & FinalState=="BIHAR"
replace district = "ROHTAS" if district=="KARAKAT" & FinalState=="BIHAR"
replace district = "SAHARSA" if district=="SUPAUL" & FinalState=="BIHAR"
replace district = "SITAMARI" if district=="SITAMARHI" & FinalState=="BIHAR"
replace district = "WEST CHAMPARAN" if district=="PASCHIM CHAMPARAN" & FinalState=="BIHAR"
replace district = "AHMEDABAD" if district=="AHMEDABAD EAST" & FinalState=="GUJARAT"
replace district = "BANS KANTHA" if district=="BANASKANTHA" & FinalState=="GUJARAT"
replace district = "PANCH MAHALS" if district=="PANCHMAHAL" & FinalState=="GUJARAT"
replace district = "BHIWANI" if district=="BHIWANI-MAHENDRAGARH" & FinalState=="HARYANA"
replace district = "HISSAR" if district=="HISAR" & FinalState=="HARYANA"
replace district = "JIND" if district=="SONIPAT" & FinalState=="HARYANA"
*replace district = "MAHENDRAGARH" if district=="BHIWANI-MAHENDRAGARH" & FinalState=="HARYANA"
replace district = "ALAPPUZM" if district=="ALAPPUZHA" & FinalState=="KERALA"
replace district = "KASARGOD" if district=="KASARAGOD" & FinalState=="KERALA"
replace district = "MALAPURAM" if district=="MALAPPURAM" & FinalState=="KERALA"
replace district = "TRICHUR" if district=="THRISSUR" & FinalState=="KERALA"
replace district = "TRIRUVANANTHAPURAM" if district=="THIRUVANANTHAPURAM" & FinalState=="KERALA"
replace district = "BID" if district=="BEED" & FinalState=="MAHARASHTRA"
replace district = "GANJAM" if district=="ASKA" & FinalState=="ORISSA"
replace district = "PHULBANI" if district=="KANDHAMAL" & FinalState=="ORISSA"
replace district = "KAPURTHALA" if district=="KHADOOR SAHIB" & FinalState=="PUNJAB"
replace district = "RUPNAGAR (ROPAR)" if district=="ANANDPUR SAHIB" & FinalState=="PUNJAB"
replace district = "BUNDI" if district=="KOTA" & FinalState=="RAJASTHAN"
replace district = "DHOLPUR" if district=="KARAULI-DHOLPUR" & FinalState=="RAJASTHAN"
replace district = "24-PARGANAS(NORTH)" if district=="BANGAON" & FinalState=="WEST BENGAL"
replace district = "24-PARGANAS(SOUTH)" if district=="JAYNAGAR" & FinalState=="WEST BENGAL"
replace district = "BURDWAN" if district=="BARDHAMAN PURBA" & FinalState=="WEST BENGAL"
replace district = "NANDIA" if district=="KRISHNANAGAR" & FinalState=="WEST BENGAL"
replace district = "WEST DINAJPUR" if district=="RAIGANJ" & FinalState=="WEST BENGAL"
