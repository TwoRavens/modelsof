#delimit ;set more off;/*NOTE: For a Mac, file path names use a forward slash (/) but for PC's they require a backward slash (\). Make these changes below if necessary depending on your computer type. */*Dropbox paths by user;if c(username)=="shayaksarkar" {;	local path "/Users/shayaksarkar/Desktop/ReStat_Data_Publication";};/*FIGURE 1*/;cd `path';use "`path'/dta/final_competition.dta", clear;/*Variable Notes*/*anyterm==the proportion of term recommendations*biasterm==0 ==> Belief=W biasterm==1 ==> Belief=T *needterm==0 ==> Need=W needterm==1 ==> Need=T *set file to which all putexcel commands will refer;putexcel set "`path'/out/figure1.xls", replace;*******************************************************/*First set of columns in figure (starting from left)*/*********************************************************Belief=W, Need=W	0.026 >> percent anyterm;tab anyterm if biasterm==0 & needterm==0 & comp==0, matcell(freq) matrow(names);putexcel A1=("Anyterm") B1=("Freq.") C1=("Percent");putexcel A2=matrix(names) B2=matrix(freq) C2=matrix(freq/r(N));*Belief=W, Need=W   0.0147 >> percent anyterm;tab anyterm if biasterm==0 & needterm==0 & comp==1, matcell(freq) matrow(names);putexcel A6=("Anyterm") B6=("Freq.") C6=("Percent");putexcel A7=matrix(names) B7=matrix(freq) C7=matrix(freq/r(N));*******************************************************/*Second set of columns in figure (starting from left)*/*********************************************************Belief=T, Need=T	0.2462	>> percent anyterm;tab anyterm if biasterm==1 & needterm==1 & comp==0, matcell(freq) matrow(names);*Belief=T, Need=T	0.2623	>> percent anyterm;putexcel A12=("Anyterm") B12=("Freq.") C12=("Percent");putexcel A13=matrix(names) B13=matrix(freq) C13=matrix(freq/r(N));tab anyterm if biasterm==1 & needterm==1 & comp==1, matcell(freq) matrow(names);putexcel A18=("Anyterm") B18=("Freq.") C18=("Percent");putexcel A19=matrix(names) B19=matrix(freq) C19=matrix(freq/r(N));*******************************************************/*Third set of columns in figure (starting from left)*/********************************************************Belief=W, Need=T	0.0933  >> percent anyterm;tab anyterm if biasterm==0 & needterm==1 & comp==0, matcell(freq) matrow(names);putexcel A24=("Anyterm") B24=("Freq.") C24=("Percent");putexcel A25=matrix(names) B25=matrix(freq) C25=matrix(freq/r(N));*Belief=W, Need=T	0.193  >> percent anyterm;tab anyterm if biasterm==0 & needterm==1 & comp==1, matcell(freq) matrow(names);putexcel A30=("Anyterm") B30=("Freq.") C30=("Percent");putexcel A31=matrix(names) B31=matrix(freq) C31=matrix(freq/r(N));********************************************************/*Fourth set of columns in figure (starting from left)*/*********************************************************Belief=T, Need=W	0.1169 >> percent anyterm;tab anyterm if biasterm==1 & needterm==0 & comp==0, matcell(freq) matrow(names);putexcel A36=("Anyterm") B36=("Freq.") C36=("Percent");putexcel A37=matrix(names) B37=matrix(freq) C37=matrix(freq/r(N));*Belief=T, Need=W	0.1169 >> percent anyterm;tab anyterm if biasterm==1 & needterm==0 & comp==1, matcell(freq) matrow(names);putexcel A42=("Anyterm") B42=("Freq.") C42=("Percent");putexcel A43=matrix(names) B43=matrix(freq) C43=matrix(freq/r(N));/*Note: Excel was used to produce figure 1 using the values produced above.*/