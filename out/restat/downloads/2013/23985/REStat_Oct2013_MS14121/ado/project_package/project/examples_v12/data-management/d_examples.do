/*
--------------------------------------------------------------------------------

Run examples from the Data-Management reference manual

--------------------------------------------------------------------------------
*/

	version 12
	

	project, do("assert.do")
	project, do("by.do")
	project, do("clonevar.do")
	project, do("codebook.do")
	project, do("collapse.do")
	project, do("compare.do")
	project, do("contract.do")
	project, do("corr2data.do")
	project, do("cross.do")
	project, do("describe.do")
	project, do("destring.do")
	project, do("drawnorm.do")
	project, do("encode.do")
	project, do("fillin.do")
	project, do("format.do")
	project, do("hexdump.do")
	project, do("icd9.do")
	project, do("import/import.do")
	project, do("infile/fixed_format/infile.do")
	project, do("infile/free_format/infile.do")
	project, do("infix/infix.do")
	project, do("insheet/insheet.do")
	project, do("joinby.do")
	project, do("mvencode.do")
	project, do("notes.do")
	project, do("outfile/outfile.do")
	project, do("range/range.do")
	project, do("recast.do")
	project, do("separate.do")
	project, do("stack/stack.do")
	project, do("statsby.do")
