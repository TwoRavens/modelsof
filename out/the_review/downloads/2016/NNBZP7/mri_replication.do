use "/Users/bleveck/Dropbox/Leveck-Kim/money, reputation, and incumbency in congressional elections/apsr response to edit/rr2/submission/Replication/mri data final.dta"/* Table 1 */felsdvreg infadjownexp1k partysd opres partysdXopres absmeandist infadjoppexp1k quality    frosh  if incumbent2==1 &  marginprev !=., ivar(stcd_redist) jvar(year) peff(cdfe) feff(yearfe) xb(mainef) res(regresid) mover(movervar)  mnum(mnumvar) pobs(pobsvar) group(coolgroupvar) cluster(stcd)felsdvreg infadjownexp1k partysd opres absmeandist opresXabsmeandist infadjoppexp1k quality    frosh  if incumbent2==1 &  marginprev !=., ivar(stcd_redist) jvar(year) peff(cdfe) feff(yearfe) xb(mainef) res(regresid) mover(movervar)  mnum(mnumvar) pobs(pobsvar) group(coolgroupvar) cluster(stcd)felsdvreg infadjownexp1k partysd opres absmeandist partysdXabsmeandist infadjoppexp1k quality    frosh  if incumbent2==1 &  marginprev !=., ivar(stcd_redist) jvar(year) peff(cdfe) feff(yearfe) xb(mainef) res(regresid) mover(movervar)  mnum(mnumvar) pobs(pobsvar) group(coolgroupvar) cluster(stcd)felsdvreg infadjownexp1k partysd opres partysdXopres absmeandist partysdXabsmeandist opresXabsmeandist  infadjoppexp1k quality    frosh  if incumbent2==1 &  marginprev !=., ivar(stcd_redist) jvar(year) peff(cdfe) feff(yearfe) xb(mainef) res(regresid) mover(movervar)  mnum(mnumvar) pobs(pobsvar) group(coolgroupvar) cluster(stcd)/* Table 2 */felsdvreg infadjownexp1k partysd opres partysdXopres bootse1  absmeandist partysdXabsmeandist opresXabsmeandist  infadjoppexp1k quality frosh  if incumbent2==1 &  marginprev !=., ivar(stcd_redist) jvar(year) peff(cdfe) feff(yearfe) xb(mainef) res(regresid) mover(movervar)  mnum(mnumvar) pobs(pobsvar) group(coolgroupvar) cluster(idno)felsdvreg infadjownexp1k partysd opres partysdXopres  nchoices absmeandist partysdXabsmeandist opresXabsmeandist  infadjoppexp1k quality frosh  if incumbent2==1 &  marginprev !=., ivar(stcd_redist) jvar(year) peff(cdfe) feff(yearfe) xb(mainef) res(regresid) mover(movervar)  mnum(mnumvar) pobs(pobsvar) group(coolgroupvar) cluster(idno)felsdvreg infadjownexp1k partysd opres partysdXopres bootse1 nchoices absmeandist partysdXabsmeandist opresXabsmeandist  infadjoppexp1k quality frosh  if incumbent2==1 &  marginprev !=., ivar(stcd_redist) jvar(year) peff(cdfe) feff(yearfe) xb(mainef) res(regresid) mover(movervar)  mnum(mnumvar) pobs(pobsvar) group(coolgroupvar) cluster(idno)