set matsize 800
set memory 900m
ivreg2 total_fdi lag_hrts lag_dem ave_years_ed BIT log_trade fdi_stock privatize labor_growth market growthrate income cap_labor_ratio fixed_xr xr_fluct _Ico* (laborrights = total_fdi log_trade fdi_stock ngos pop_log economicpeerspractices regionalpractices externaldebt income growthrate lag_dem civilwar), cluster(countrynumber)
ivreg2 primary_fdi lag_hrts lag_dem ave_years_ed BIT log_trade fdi_stock privatize labor_growth market growthrate income cap_labor_ratio fixed_xr xr_fluct _Ico* (laborrights = primary_fdi log_trade fdi_stock ngos pop_log economicpeerspractices regionalpractices externaldebt income growthrate lag_dem civilwar), cluster(countrynumber)
ivreg2 secondary_fdi lag_hrts lag_dem ave_years_ed BIT log_trade fdi_stock privatize labor_growth market growthrate income cap_labor_ratio fixed_xr xr_fluct _Ico* (laborrights = secondary_fdi log_trade fdi_stock ngos pop_log economicpeerspractices regionalpractices externaldebt income growthrate lag_dem civilwar), cluster(countrynumber)
ivreg2 service_fdi lag_hrts lag_dem ave_years_ed BIT log_trade fdi_stock privatize labor_growth market growthrate income cap_labor_ratio fixed_xr xr_fluct _Ico* (laborrights = service_fdi log_trade fdi_stock ngos pop_log economicpeerspractices regionalpractices externaldebt income growthrate lag_dem civilwar), cluster(countrynumber)
ivreg2 laborrights log_trade fdi_stock ngos pop_log economicpeerspractices regionalpractices externaldebt income growthrate lag_dem civilwar _Ico* (total_fdi = laborrights lag_hrts lag_dem ave_years_ed BIT log_trade fdi_stock privatize labor_growth market growthrate income cap_labor_ratio fixed_xr xr_fluct), cluster(countrynumber)
ivreg2 laborrights log_trade fdi_stock ngos pop_log economicpeerspractices regionalpractices externaldebt income growthrate lag_dem civilwar _Ico* (primary_fdi = laborrights lag_hrts lag_dem ave_years_ed BIT log_trade fdi_stock privatize labor_growth market growthrate income cap_labor_ratio fixed_xr xr_fluct), cluster(countrynumber)
ivreg2 laborrights log_trade fdi_stock ngos pop_log economicpeerspractices regionalpractices externaldebt income growthrate lag_dem civilwar _Ico* (secondary_fdi = laborrights lag_hrts lag_dem ave_years_ed BIT log_trade fdi_stock privatize labor_growth market growthrate income cap_labor_ratio fixed_xr xr_fluct), cluster(countrynumber)
ivreg2 laborrights log_trade fdi_stock ngos pop_log economicpeerspractices regionalpractices externaldebt income growthrate lag_dem civilwar _Ico* (service_fdi = laborrights lag_hrts lag_dem ave_years_ed BIT log_trade fdi_stock privatize labor_growth market growthrate income cap_labor_ratio fixed_xr xr_fluct), cluster(countrynumber)
