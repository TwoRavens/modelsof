* "Wanting What is Fair:  How Party Cues and Information about ///* Income Inequality Affect Public Support for Taxes"*Authors:  Cheryl Boudreau and Scott A. MacKenzie*Replication of results in the Online Appendix*Validity of our Measure of Citizens' Beliefs about the Ideal Income Distribution*Egalitarianism and citizens' beliefs about the ideal income distribution*These results are shown in Table A1.*Model for "Ideal Income Bottom 40%"reg Q4_Q5_ideal egal_index_pos if (dem==1 | dem==0)*Model for "Ideal Income Poorest 20%"reg Q5_ideal egal_index_pos if (dem==1 | dem==0)*Model for "Ideal Income Richest 20%"reg Q1_ideal egal_index_pos if (dem==1 | dem==0)*Model for "Poor Have Less Than Ideally Should" (without controls)logit p_less egal_index_pos if (dem==1 | dem==0)*Model for "Poor Have Less Than Ideally Should" (with controls)logit p_less egal_index_pos low_inc college know_hi if (dem==1 | dem==0)*Predictive Validity of Citizens' Beliefs about the Ideal Income Distribution*These results are shown in Table A2.  reg tax_opin p_less low_inc know_hi if (dem==0 | dem==1)  *Priming and Citizens' Tax Policy Opinions*Results for Respondents with Intervening Questions (Minimal Priming)*These results are shown in Figure A1.*Republicans' support for the progressive income tax in the control/treatment groupssum sup_prog if dem==0 & treat==1 & inter_quest==1sum sup_prog if dem==0 & treat==4 & inter_quest==1sum sup_prog if dem==0 & treat==3 & inter_quest==1sum sup_prog if dem==0 & treat==2 & inter_quest==1*T-tests for Republicans' support for the progressive income tax*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==0 & inter_quest==1 & (treat==1 | treat==4), by(treat)ttest sup_prog if dem==0 & inter_quest==1 & (treat==1 | treat==3), by(treat)ttest sup_prog if dem==0 & inter_quest==1 & (treat==1 | treat==2), by(treat)*Democrats' support for the progressive income tax in the control/treatment groupssum sup_prog if dem==1 & treat==1 & inter_quest==1sum sup_prog if dem==1 & treat==4 & inter_quest==1sum sup_prog if dem==1 & treat==3 & inter_quest==1sum sup_prog if dem==1 & treat==2 & inter_quest==1*T-tests for Democrats' support for the progressive income tax*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==1 & inter_quest==1 & (treat==1 | treat==4), by(treat)ttest sup_prog if dem==1 & inter_quest==1 & (treat==1 | treat==3), by(treat)ttest sup_prog if dem==1 & inter_quest==1 & (treat==1 | treat==2), by(treat)*Republicans' support for the sales tax in the control/treatment groupssum sup_sales if dem==0 & treat==1 & inter_quest==1sum sup_sales if dem==0 & treat==4 & inter_quest==1sum sup_sales if dem==0 & treat==3 & inter_quest==1sum sup_sales if dem==0 & treat==2 & inter_quest==1*T-tests for Republicans' support for the sales tax*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==0 & inter_quest==1 & (treat==1 | treat==4), by(treat)ttest sup_sales if dem==0 & inter_quest==1 & (treat==1 | treat==3), by(treat)ttest sup_sales if dem==0 & inter_quest==1 & (treat==1 | treat==2), by(treat)*Democrats' support for the sales tax in the control/treatment groupssum sup_sales if dem==1 & treat==1 & inter_quest==1sum sup_sales if dem==1 & treat==4 & inter_quest==1sum sup_sales if dem==1 & treat==3 & inter_quest==1sum sup_sales if dem==1 & treat==2 & inter_quest==1*T-tests for Democrats' support for the sales tax*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==1 & inter_quest==1 & (treat==1 | treat==4), by(treat)ttest sup_sales if dem==1 & inter_quest==1 & (treat==1 | treat==3), by(treat)ttest sup_sales if dem==1 & inter_quest==1 & (treat==1 | treat==2), by(treat)*Results for Respondents Without Intervening Questions*These results are shown in Figure A2.*Republicans' support for the progressive income tax in the control/treatment groupssum sup_prog if dem==0 & treat==1 & inter_quest==2sum sup_prog if dem==0 & treat==4 & inter_quest==2sum sup_prog if dem==0 & treat==3 & inter_quest==2sum sup_prog if dem==0 & treat==2 & inter_quest==2*T-tests for Republicans' support for the progressive income tax*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==0 & inter_quest==2 & (treat==1 | treat==4), by(treat)ttest sup_prog if dem==0 & inter_quest==2 & (treat==1 | treat==3), by(treat)ttest sup_prog if dem==0 & inter_quest==2 & (treat==1 | treat==2), by(treat)*Democrats' support for the progressive income tax in the control/treatment groupssum sup_prog if dem==1 & treat==1 & inter_quest==2sum sup_prog if dem==1 & treat==4 & inter_quest==2sum sup_prog if dem==1 & treat==3 & inter_quest==2sum sup_prog if dem==1 & treat==2 & inter_quest==2*T-tests for Democrats' support for the progressive income tax*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==1 & inter_quest==2 & (treat==1 | treat==4), by(treat)ttest sup_prog if dem==1 & inter_quest==2 & (treat==1 | treat==3), by(treat)ttest sup_prog if dem==1 & inter_quest==2 & (treat==1 | treat==2), by(treat)*Republicans' support for the sales tax in the control/treatment groupssum sup_sales if dem==0 & treat==1 & inter_quest==2sum sup_sales if dem==0 & treat==4 & inter_quest==2sum sup_sales if dem==0 & treat==3 & inter_quest==2sum sup_sales if dem==0 & treat==2 & inter_quest==2*T-tests for Republicans' support for the sales tax*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==0 & inter_quest==2 & (treat==1 | treat==4), by(treat)ttest sup_sales if dem==0 & inter_quest==2 & (treat==1 | treat==3), by(treat)ttest sup_sales if dem==0 & inter_quest==2 & (treat==1 | treat==2), by(treat)*Democrats' support for the sales tax in the control/treatment groupssum sup_sales if dem==1 & treat==1 & inter_quest==2sum sup_sales if dem==1 & treat==4 & inter_quest==2sum sup_sales if dem==1 & treat==3 & inter_quest==2sum sup_sales if dem==1 & treat==2 & inter_quest==2*T-tests for Democrats' support for the sales tax*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==1 & inter_quest==2 & (treat==1 | treat==4), by(treat)ttest sup_sales if dem==1 & inter_quest==2 & (treat==1 | treat==3), by(treat)ttest sup_sales if dem==1 & inter_quest==2 & (treat==1 | treat==2), by(treat)*Four-Valued Indicator of Support for Tax Policies*Support for Progressive Income Tax and Regressive Sales Tax Increase with ///*Four-Valued Indicator of Support*These results are shown in Figure A3.*Republicans' support for the progressive income tax in the control/treatment groupssum opin_prog if dem==0 & treat==1sum opin_prog if dem==0 & treat==4sum opin_prog if dem==0 & treat==3sum opin_prog if dem==0 & treat==2*T-tests for Republicans' support for the progressive income tax*Control versus party cues, inequality info, and party cues + inequality infottest opin_prog if dem==0 & (treat==1 | treat==4), by(treat)ttest opin_prog if dem==0 & (treat==1 | treat==3), by(treat)ttest opin_prog if dem==0 & (treat==1 | treat==2), by(treat)*Democrats' support for the progressive income tax in the control/treatment groupssum opin_prog if dem==1 & treat==1sum opin_prog if dem==1 & treat==4sum opin_prog if dem==1 & treat==3sum opin_prog if dem==1 & treat==2*T-tests for Democrats' support for the progressive income tax*Control versus party cues, inequality info, and party cues + inequality infottest opin_prog if dem==1 & (treat==1 | treat==4), by(treat)ttest opin_prog if dem==1 & (treat==1 | treat==3), by(treat)ttest opin_prog if dem==1 & (treat==1 | treat==2), by(treat)*Republicans' support for the sales tax in the control/treatment groupssum opin_sales if dem==0 & treat==1sum opin_sales if dem==0 & treat==4sum opin_sales if dem==0 & treat==3sum opin_sales if dem==0 & treat==2*T-tests for Republicans' support for the sales tax*Control versus party cues, inequality info, and party cues + inequality infottest opin_sales if dem==0 & (treat==1 | treat==4), by(treat)ttest opin_sales if dem==0 & (treat==1 | treat==3), by(treat)ttest opin_sales if dem==0 & (treat==1 | treat==2), by(treat)*Democrats' support for the sales tax in the control/treatment groupssum opin_sales if dem==1 & treat==1sum opin_sales if dem==1 & treat==4sum opin_sales if dem==1 & treat==3sum opin_sales if dem==1 & treat==2*T-tests for Democrats' support for the progressive income tax*Control versus party cues, inequality info, and party cues + inequality infottest opin_sales if dem==1 & (treat==1 | treat==4), by(treat)ttest opin_sales if dem==1 & (treat==1 | treat==3), by(treat)ttest opin_sales if dem==1 & (treat==1 | treat==2), by(treat)*Support for Taxes by Beliefs about the Income Held by the Poor with ///*Four-Valued Indicator of Support*These results are shown in Figure A4.*Republicans' support for the progressive income tax in the control/treatment groups ///*among those who believe the poor have less than they shouldsum opin_prog if dem==0 & treat==1 & p_less==1 sum opin_prog if dem==0 & treat==4 & p_less==1sum opin_prog if dem==0 & treat==3 & p_less==1sum opin_prog if dem==0 & treat==2 & p_less==1*T-tests for these Republicans' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest opin_prog if dem==0 & p_less==1 & (treat==1 | treat==4), by(treat)ttest opin_prog if dem==0 & p_less==1 & (treat==1 | treat==3), by(treat)ttest opin_prog if dem==0 & p_less==1 & (treat==1 | treat==2), by(treat)*Republicans' support for the progressive income tax in the control/treatment groups ///*among those who believe the poor have as much or more than they shouldsum opin_prog if dem==0 & treat==1 & p_less==0 sum opin_prog if dem==0 & treat==4 & p_less==0sum opin_prog if dem==0 & treat==3 & p_less==0sum opin_prog if dem==0 & treat==2 & p_less==0*T-tests for these Republicans' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest opin_prog if dem==0 & p_less==0 & (treat==1 | treat==4), by(treat)ttest opin_prog if dem==0 & p_less==0 & (treat==1 | treat==3), by(treat)ttest opin_prog if dem==0 & p_less==0 & (treat==1 | treat==2), by(treat)*Democrats' support for the progressive income tax in the control/treatment groups ///*among those who believe the poor have less than they shouldsum opin_prog if dem==1 & treat==1 & p_less==1 sum opin_prog if dem==1 & treat==4 & p_less==1sum opin_prog if dem==1 & treat==3 & p_less==1sum opin_prog if dem==1 & treat==2 & p_less==1*T-tests for these Democrats' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest opin_prog if dem==1 & p_less==1 & (treat==1 | treat==4), by(treat)ttest opin_prog if dem==1 & p_less==1 & (treat==1 | treat==3), by(treat)ttest opin_prog if dem==1 & p_less==1 & (treat==1 | treat==2), by(treat)*Democrats' support for the progressive income tax in the control/treatment groups ///*among those who believe the poor have as much or more than they shouldsum opin_prog if dem==1 & treat==1 & p_less==0 sum opin_prog if dem==1 & treat==4 & p_less==0sum opin_prog if dem==1 & treat==3 & p_less==0sum opin_prog if dem==1 & treat==2 & p_less==0*T-tests for these Democrats' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest opin_prog if dem==1 & p_less==0 & (treat==1 | treat==4), by(treat)ttest opin_prog if dem==1 & p_less==0 & (treat==1 | treat==3), by(treat)ttest opin_prog if dem==1 & p_less==0 & (treat==1 | treat==2), by(treat)*Republicans' support for the sales tax in the control/treatment groups ///*among those who believe the poor have less than they shouldsum opin_sales if dem==0 & treat==1 & p_less==1 sum opin_sales if dem==0 & treat==4 & p_less==1sum opin_sales if dem==0 & treat==3 & p_less==1sum opin_sales if dem==0 & treat==2 & p_less==1*T-tests for these Republicans' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest opin_sales if dem==0 & p_less==1 & (treat==1 | treat==4), by(treat)ttest opin_sales if dem==0 & p_less==1 & (treat==1 | treat==3), by(treat)ttest opin_sales if dem==0 & p_less==1 & (treat==1 | treat==2), by(treat)*Republicans' support for the sales tax in the control/treatment groups ///*among those who believe the poor have as much or more than they shouldsum opin_sales if dem==0 & treat==1 & p_less==0 sum opin_sales if dem==0 & treat==4 & p_less==0sum opin_sales if dem==0 & treat==3 & p_less==0sum opin_sales if dem==0 & treat==2 & p_less==0*T-tests for these Republicans' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest opin_sales if dem==0 & p_less==0 & (treat==1 | treat==4), by(treat)ttest opin_sales if dem==0 & p_less==0 & (treat==1 | treat==3), by(treat)ttest opin_sales if dem==0 & p_less==0 & (treat==1 | treat==2), by(treat)*Democrats' support for the sales tax in the control/treatment groups ///*among those who believe the poor have less than they shouldsum opin_sales if dem==1 & treat==1 & p_less==1 sum opin_sales if dem==1 & treat==4 & p_less==1sum opin_sales if dem==1 & treat==3 & p_less==1sum opin_sales if dem==1 & treat==2 & p_less==1*T-tests for these Democrats' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest opin_sales if dem==1 & p_less==1 & (treat==1 | treat==4), by(treat)ttest opin_sales if dem==1 & p_less==1 & (treat==1 | treat==3), by(treat)ttest opin_sales if dem==1 & p_less==1 & (treat==1 | treat==2), by(treat)*Democrats' support for the sales tax in the control/treatment groups ///*among those who believe the poor have as much or more than they shouldsum opin_sales if dem==1 & treat==1 & p_less==0 sum opin_sales if dem==1 & treat==4 & p_less==0sum opin_sales if dem==1 & treat==3 & p_less==0sum opin_sales if dem==1 & treat==2 & p_less==0*T-tests for these Democrats' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest opin_sales if dem==1 & p_less==0 & (treat==1 | treat==4), by(treat)ttest opin_sales if dem==1 & p_less==0 & (treat==1 | treat==3), by(treat)ttest opin_sales if dem==1 & p_less==0 & (treat==1 | treat==2), by(treat)*Support for Taxes by Beliefs about the Income Held by the Rich*These results are shown in Figure A5.*Republicans' support for the progressive income tax in the control/treatment groups ///*among those who believe the rich have more than they shouldsum sup_prog if dem==0 & treat==1 & r_more==1sum sup_prog if dem==0 & treat==4 & r_more==1sum sup_prog if dem==0 & treat==3 & r_more==1sum sup_prog if dem==0 & treat==2 & r_more==1*T-tests for these Republicans' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==0 & r_more==1 & (treat==1 | treat==4), by(treat)ttest sup_prog if dem==0 & r_more==1 & (treat==1 | treat==3), by(treat)ttest sup_prog if dem==0 & r_more==1 & (treat==1 | treat==2), by(treat)*Republicans' support for the progressive income tax in the control/treatment groups ///*among those who believe the rich have as much as or less income than they shouldsum sup_prog if dem==0 & treat==1 & r_more==0sum sup_prog if dem==0 & treat==4 & r_more==0sum sup_prog if dem==0 & treat==3 & r_more==0sum sup_prog if dem==0 & treat==2 & r_more==0*T-tests for these Republicans' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==0 & r_more==0 & (treat==1 | treat==4), by(treat)ttest sup_prog if dem==0 & r_more==0 & (treat==1 | treat==3), by(treat)ttest sup_prog if dem==0 & r_more==0 & (treat==1 | treat==2), by(treat)*Democrats' support for the progressive income tax in the control/treatment groups ///*among those who believe the rich have more than they shouldsum sup_prog if dem==1 & treat==1 & r_more==1sum sup_prog if dem==1 & treat==4 & r_more==1sum sup_prog if dem==1 & treat==3 & r_more==1sum sup_prog if dem==1 & treat==2 & r_more==1*T-tests for these Democrats' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==1 & r_more==1 & (treat==1 | treat==4), by(treat)ttest sup_prog if dem==1 & r_more==1 & (treat==1 | treat==3), by(treat)ttest sup_prog if dem==1 & r_more==1 & (treat==1 | treat==2), by(treat)*Democrats' support for the progressive income tax in the control/treatment groups ///*among those who believe the rich have as much as or less income than they shouldsum sup_prog if dem==1 & treat==1 & r_more==0sum sup_prog if dem==1 & treat==4 & r_more==0sum sup_prog if dem==1 & treat==3 & r_more==0sum sup_prog if dem==1 & treat==2 & r_more==0*T-tests for these Democrats' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==1 & r_more==0 & (treat==1 | treat==4), by(treat)ttest sup_prog if dem==1 & r_more==0 & (treat==1 | treat==3), by(treat)ttest sup_prog if dem==1 & r_more==0 & (treat==1 | treat==2), by(treat)*Republicans' support for the sales tax in the control/treatment groups ///*among those who believe the rich have more than they shouldsum sup_sales if dem==0 & treat==1 & r_more==1sum sup_sales if dem==0 & treat==4 & r_more==1sum sup_sales if dem==0 & treat==3 & r_more==1sum sup_sales if dem==0 & treat==2 & r_more==1*T-tests for these Republicans' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==0 & r_more==1 & (treat==1 | treat==4), by(treat)ttest sup_sales if dem==0 & r_more==1 & (treat==1 | treat==3), by(treat)ttest sup_sales if dem==0 & r_more==1 & (treat==1 | treat==2), by(treat)*Republicans' support for the sales tax in the control/treatment groups ///*among those who believe the rich have as much as or less income than they shouldsum sup_sales if dem==0 & treat==1 & r_more==0sum sup_sales if dem==0 & treat==4 & r_more==0sum sup_sales if dem==0 & treat==3 & r_more==0sum sup_sales if dem==0 & treat==2 & r_more==0*T-tests for these Republicans' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==0 & r_more==0 & (treat==1 | treat==4), by(treat)ttest sup_sales if dem==0 & r_more==0 & (treat==1 | treat==3), by(treat)ttest sup_sales if dem==0 & r_more==0 & (treat==1 | treat==2), by(treat)*Democrats' support for the sales tax in the control/treatment groups ///*among those who believe the rich have more than they shouldsum sup_sales if dem==1 & treat==1 & r_more==1sum sup_sales if dem==1 & treat==4 & r_more==1sum sup_sales if dem==1 & treat==3 & r_more==1sum sup_sales if dem==1 & treat==2 & r_more==1*T-tests for these Democrats' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==1 & r_more==1 & (treat==1 | treat==4), by(treat)ttest sup_sales if dem==1 & r_more==1 & (treat==1 | treat==3), by(treat)ttest sup_sales if dem==1 & r_more==1 & (treat==1 | treat==2), by(treat)*Democrats' support for the sales tax in the control/treatment groups ///*among those who believe the rich have as much as or less income than they shouldsum sup_sales if dem==1 & treat==1 & r_more==0sum sup_sales if dem==1 & treat==4 & r_more==0sum sup_sales if dem==1 & treat==3 & r_more==0sum sup_sales if dem==1 & treat==2 & r_more==0*T-tests for these Democrats' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==1 & r_more==0 & (treat==1 | treat==4), by(treat)ttest sup_sales if dem==1 & r_more==0 & (treat==1 | treat==3), by(treat)ttest sup_sales if dem==1 & r_more==0 & (treat==1 | treat==2), by(treat)*Estimating Reaction Times Using a Cox Proportional Hazards Model*Cox Proportional Hazards Model of All Respondents' Reaction Times (Logged Seconds)*These results are shown in Table A3.*All respondents on progressive income taxstset prog_rt_logstcox party ineq par_ineq if (dem==1 | dem==0), nohrstcox party ineq par_ineq if (dem==1 | dem==0)*All respondents on regressive sales taxstset sales_rt_logstcox party ineq par_ineq if (dem==1 | dem==0), nohrstcox party ineq par_ineq if (dem==1 | dem==0)*Cox Proportional Hazards Model of Republican and Democratic Respondents' ///*Reaction Times (Logged Seconds)*These results are shown in Table A4.*Republicans on progressive income taxstset prog_rt_logstcox party ineq par_ineq if (dem==0), nohrstcox party ineq par_ineq if (dem==0)*Democrats on regressive sales tax stset sales_rt_logstcox party ineq par_ineq if (dem==1), nohrstcox party ineq par_ineq if (dem==1)*Actual, Estimated, and Ideal Distributions of Income in California*These results are shown in Table A5.*Note:  Data on the actual income distribution in California in 2009 ///was drawn from the Franchise Tax Board of California*Democrats' estimates of actual levels of inequalitysum Q1_real if dem==1 sum Q2_real if dem==1 sum Q3_real if dem==1 sum Q4_real if dem==1 sum Q5_real if dem==1 *Democrats' beliefs about ideal levels of inequalitysum Q1_ideal if dem==1 sum Q2_ideal if dem==1 sum Q3_ideal if dem==1 sum Q4_ideal if dem==1 sum Q5_ideal if dem==1 *Republicans' estimates of actual levels of inequalitysum Q1_real if dem==0sum Q2_real if dem==0 sum Q3_real if dem==0 sum Q4_real if dem==0 sum Q5_real if dem==0 *Republicans' beliefs about ideal levels of inequalitysum Q1_ideal if dem==0sum Q2_ideal if dem==0 sum Q3_ideal if dem==0 sum Q4_ideal if dem==0 sum Q5_ideal if dem==0 *Support for Progressive Income Tax and Regressive Sales Tax Increase*These results are shown in Table A6.*Democrats' support for the progressive income tax in the control/treatment groupssum sup_prog if dem==1 & treat==1 sum sup_prog if dem==1 & treat==4 sum sup_prog if dem==1 & treat==3 sum sup_prog if dem==1 & treat==2 *T-tests for Democrats' support for the progressive income tax*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==1 & (treat==1 | treat==4), by(treat)ttest sup_prog if dem==1 & (treat==1 | treat==3), by(treat)ttest sup_prog if dem==1 & (treat==1 | treat==2), by(treat)*Republicans' support for the progressive income tax in the control/treatment groupssum sup_prog if dem==0 & treat==1 sum sup_prog if dem==0 & treat==4 sum sup_prog if dem==0 & treat==3 sum sup_prog if dem==0 & treat==2 *T-tests for Republicans' support for the progressive income tax*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==0 & (treat==1 | treat==4), by(treat)ttest sup_prog if dem==0 & (treat==1 | treat==3), by(treat)ttest sup_prog if dem==0 & (treat==1 | treat==2), by(treat)*Democrats' support for the sales tax in the control/treatment groupssum sup_sales if dem==1 & treat==1sum sup_sales if dem==1 & treat==4sum sup_sales if dem==1 & treat==3sum sup_sales if dem==1 & treat==2*T-tests for Democrats' support for the sales tax*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==1 & (treat==1 | treat==4), by(treat)ttest sup_sales if dem==1 & (treat==1 | treat==3), by(treat)ttest sup_sales if dem==1 & (treat==1 | treat==2), by(treat)*Republicans' support for the sales tax in the control/treatment groupssum sup_sales if dem==0 & treat==1sum sup_sales if dem==0 & treat==4sum sup_sales if dem==0 & treat==3sum sup_sales if dem==0 & treat==2*T-tests for Republicans' support for the sales tax*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==0 & (treat==1 | treat==4), by(treat)ttest sup_sales if dem==0 & (treat==1 | treat==3), by(treat)ttest sup_sales if dem==0 & (treat==1 | treat==2), by(treat)*Support for Progressive Income Tax by Beliefs about Income Inequality*These results are shown in Table A7.*Democrats' support for the progressive income tax in the control/treatment groups ///*among those who believe the poor have less than they shouldsum sup_prog if dem==1 & treat==1 & p_less==1sum sup_prog if dem==1 & treat==4 & p_less==1sum sup_prog if dem==1 & treat==3 & p_less==1sum sup_prog if dem==1 & treat==2 & p_less==1*T-tests for these Democrats' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==1 & p_less==1 & (treat==1 | treat==4), by(treat)ttest sup_prog if dem==1 & p_less==1 & (treat==1 | treat==3), by(treat)ttest sup_prog if dem==1 & p_less==1 & (treat==1 | treat==2), by(treat)*Democrats' support for the progressive income tax in the control/treatment groups ///*among those who believe the poor have as much as or more than they shouldsum sup_prog if dem==1 & treat==1 & p_less==0sum sup_prog if dem==1 & treat==4 & p_less==0sum sup_prog if dem==1 & treat==3 & p_less==0sum sup_prog if dem==1 & treat==2 & p_less==0*T-tests for these Democrats' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==1 & p_less==0 & (treat==1 | treat==4), by(treat)ttest sup_prog if dem==1 & p_less==0 & (treat==1 | treat==3), by(treat)ttest sup_prog if dem==1 & p_less==0 & (treat==1 | treat==2), by(treat)*Republicans' support for the progressive income tax in the control/treatment groups ///*among those who believe the poor have less than they shouldsum sup_prog if dem==0 & treat==1 & p_less==1sum sup_prog if dem==0 & treat==4 & p_less==1sum sup_prog if dem==0 & treat==3 & p_less==1sum sup_prog if dem==0 & treat==2 & p_less==1*T-tests for these Republicans' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==0 & p_less==1 & (treat==1 | treat==4), by(treat)ttest sup_prog if dem==0 & p_less==1 & (treat==1 | treat==3), by(treat)ttest sup_prog if dem==0 & p_less==1 & (treat==1 | treat==2), by(treat)*Republicans' support for the progressive income tax in the control/treatment groups ///*among those who believe the poor have as much as or more than they shouldsum sup_prog if dem==0 & treat==1 & p_less==0sum sup_prog if dem==0 & treat==4 & p_less==0sum sup_prog if dem==0 & treat==3 & p_less==0sum sup_prog if dem==0 & treat==2 & p_less==0*T-tests for these Republicans' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==0 & p_less==0 & (treat==1 | treat==4), by(treat)ttest sup_prog if dem==0 & p_less==0 & (treat==1 | treat==3), by(treat)ttest sup_prog if dem==0 & p_less==0 & (treat==1 | treat==2), by(treat)*Support for Regressive Sales Tax by Beliefs about Income Inequality*These results are shown in Table A8.*Democrats' support for the sales tax in the control/treatment groups ///*among those who believe the poor have less than they shouldsum sup_sales if dem==1 & treat==1 & p_less==1sum sup_sales if dem==1 & treat==4 & p_less==1sum sup_sales if dem==1 & treat==3 & p_less==1sum sup_sales if dem==1 & treat==2 & p_less==1*T-tests for these Democrats' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==1 & p_less==1 & (treat==1 | treat==4), by(treat)ttest sup_sales if dem==1 & p_less==1 & (treat==1 | treat==3), by(treat)ttest sup_sales if dem==1 & p_less==1 & (treat==1 | treat==2), by(treat)*Democrats' support for the sales tax in the control/treatment groups ///*among those who believe the poor have as much as or more than they shouldsum sup_sales if dem==1 & treat==1 & p_less==0sum sup_sales if dem==1 & treat==4 & p_less==0sum sup_sales if dem==1 & treat==3 & p_less==0sum sup_sales if dem==1 & treat==2 & p_less==0*T-tests for these Democrats' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==1 & p_less==0 & (treat==1 | treat==4), by(treat)ttest sup_sales if dem==1 & p_less==0 & (treat==1 | treat==3), by(treat)ttest sup_sales if dem==1 & p_less==0 & (treat==1 | treat==2), by(treat)*Republicans' support for the sales tax in the control/treatment groups ///*among those who believe the poor have less than they shouldsum sup_sales if dem==0 & treat==1 & p_less==1sum sup_sales if dem==0 & treat==4 & p_less==1sum sup_sales if dem==0 & treat==3 & p_less==1sum sup_sales if dem==0 & treat==2 & p_less==1*T-tests for these Republicans' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==0 & p_less==1 & (treat==1 | treat==4), by(treat)ttest sup_sales if dem==0 & p_less==1 & (treat==1 | treat==3), by(treat)ttest sup_sales if dem==0 & p_less==1 & (treat==1 | treat==2), by(treat)*Republicans' support for the sales tax in the control/treatment groups ///*among those who believe the poor have as much as or more than they shouldsum sup_sales if dem==0 & treat==1 & p_less==0sum sup_sales if dem==0 & treat==4 & p_less==0sum sup_sales if dem==0 & treat==3 & p_less==0sum sup_sales if dem==0 & treat==2 & p_less==0*T-tests for these Republicans' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==0 & p_less==0 & (treat==1 | treat==4), by(treat)ttest sup_sales if dem==0 & p_less==0 & (treat==1 | treat==3), by(treat)ttest sup_sales if dem==0 & p_less==0 & (treat==1 | treat==2), by(treat)*Support for Taxes by Political Ideology*These results are shown in Figure A6.*Conservatives' support for the progressive income tax in the control/treatment groupssum sup_prog if liberal==0 & treat==1 sum sup_prog if liberal==0 & treat==4 sum sup_prog if liberal==0 & treat==3 sum sup_prog if liberal==0 & treat==2 *T-tests for conservatives' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if liberal==0 & (treat==1 | treat==4), by(treat)ttest sup_prog if liberal==0 & (treat==1 | treat==3), by(treat)ttest sup_prog if liberal==0 & (treat==1 | treat==2), by(treat)*Liberals' support for the progressive income tax in the control/treatment groupssum sup_prog if liberal==1 & treat==1 sum sup_prog if liberal==1 & treat==4 sum sup_prog if liberal==1 & treat==3 sum sup_prog if liberal==1 & treat==2 *T-tests for liberals' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if liberal==1 & (treat==1 | treat==4), by(treat)ttest sup_prog if liberal==1 & (treat==1 | treat==3), by(treat)ttest sup_prog if liberal==1 & (treat==1 | treat==2), by(treat)*Conservatives' support for the sales tax in the control/treatment groupssum sup_sales if liberal==0 & treat==1 sum sup_sales if liberal==0 & treat==4 sum sup_sales if liberal==0 & treat==3 sum sup_sales if liberal==0 & treat==2 *T-tests for conservatives' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if liberal==0 & (treat==1 | treat==4), by(treat)ttest sup_sales if liberal==0 & (treat==1 | treat==3), by(treat)ttest sup_sales if liberal==0 & (treat==1 | treat==2), by(treat)*Liberals' support for the sales tax in the control/treatment groupssum sup_sales if liberal==1 & treat==1 sum sup_sales if liberal==1 & treat==4 sum sup_sales if liberal==1 & treat==3 sum sup_sales if liberal==1 & treat==2 *T-tests for liberals' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if liberal==1 & (treat==1 | treat==4), by(treat)ttest sup_sales if liberal==1 & (treat==1 | treat==3), by(treat)ttest sup_sales if liberal==1 & (treat==1 | treat==2), by(treat)*Support for Taxes by Level of Education*These results are shown in Figure A7.*Republicans' support for the progressive income tax in the control/treatment groups ///*among those with a two-year college degree or greatersum sup_prog if dem==0 & treat==1 & college==1sum sup_prog if dem==0 & treat==4 & college==1sum sup_prog if dem==0 & treat==3 & college==1sum sup_prog if dem==0 & treat==2 & college==1*T-tests for these Republicans' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==0 & (treat==1 | treat==4) & college==1, by(treat)ttest sup_prog if dem==0 & (treat==1 | treat==3) & college==1, by(treat)ttest sup_prog if dem==0 & (treat==1 | treat==2) & college==1, by(treat)*Republicans' support for the progressive income tax in the control/treatment groups ///*among those with no college degreesum sup_prog if dem==0 & treat==1 & college==0sum sup_prog if dem==0 & treat==4 & college==0sum sup_prog if dem==0 & treat==3 & college==0sum sup_prog if dem==0 & treat==2 & college==0*T-tests for these Republicans' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==0 & (treat==1 | treat==4) & college==0, by(treat)ttest sup_prog if dem==0 & (treat==1 | treat==3) & college==0, by(treat)ttest sup_prog if dem==0 & (treat==1 | treat==2) & college==0, by(treat)*Democrats' support for the progressive income tax in the control/treatment groups ///*among those with a two-year college degree or greatersum sup_prog if dem==1 & treat==1 & college==1sum sup_prog if dem==1 & treat==4 & college==1sum sup_prog if dem==1 & treat==3 & college==1sum sup_prog if dem==1 & treat==2 & college==1*T-tests for these Democrats' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==1 & (treat==1 | treat==4) & college==1, by(treat)ttest sup_prog if dem==1 & (treat==1 | treat==3) & college==1, by(treat)ttest sup_prog if dem==1 & (treat==1 | treat==2) & college==1, by(treat)*Democrats' support for the progressive income tax in the control/treatment groups ///*among those with no college degreesum sup_prog if dem==1 & treat==1 & college==0sum sup_prog if dem==1 & treat==4 & college==0sum sup_prog if dem==1 & treat==3 & college==0sum sup_prog if dem==1 & treat==2 & college==0*T-tests for these Democrats' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==1 & (treat==1 | treat==4) & college==0, by(treat)ttest sup_prog if dem==1 & (treat==1 | treat==3) & college==0, by(treat)ttest sup_prog if dem==1 & (treat==1 | treat==2) & college==0, by(treat)*Republicans' support for the sales tax in the control/treatment groups ///*among those with a two-year college degree or greatersum sup_sales if dem==0 & treat==1 & college==1sum sup_sales if dem==0 & treat==4 & college==1sum sup_sales if dem==0 & treat==3 & college==1sum sup_sales if dem==0 & treat==2 & college==1*T-tests for these Republicans' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==0 & (treat==1 | treat==4) & college==1, by(treat)ttest sup_sales if dem==0 & (treat==1 | treat==3) & college==1, by(treat)ttest sup_sales if dem==0 & (treat==1 | treat==2) & college==1, by(treat)*Republicans' support for the sales tax in the control/treatment groups ///*among those with no college degreesum sup_sales if dem==0 & treat==1 & college==0sum sup_sales if dem==0 & treat==4 & college==0sum sup_sales if dem==0 & treat==3 & college==0sum sup_sales if dem==0 & treat==2 & college==0*T-tests for these Republicans' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==0 & (treat==1 | treat==4) & college==0, by(treat)ttest sup_sales if dem==0 & (treat==1 | treat==3) & college==0, by(treat)ttest sup_sales if dem==0 & (treat==1 | treat==2) & college==0, by(treat)*Democrats' support for the sales tax in the control/treatment groups ///*among those with a two-year college degree or greatersum sup_sales if dem==1 & treat==1 & college==1sum sup_sales if dem==1 & treat==4 & college==1sum sup_sales if dem==1 & treat==3 & college==1sum sup_sales if dem==1 & treat==2 & college==1*T-tests for these Democrats' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==1 & (treat==1 | treat==4) & college==1, by(treat)ttest sup_sales if dem==1 & (treat==1 | treat==3) & college==1, by(treat)ttest sup_sales if dem==1 & (treat==1 | treat==2) & college==1, by(treat)*Democrats' support for the sales tax in the control/treatment groups ///*among those with no college degreesum sup_sales if dem==1 & treat==1 & college==0sum sup_sales if dem==1 & treat==4 & college==0sum sup_sales if dem==1 & treat==3 & college==0sum sup_sales if dem==1 & treat==2 & college==0*T-tests for these Democrats' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==1 & (treat==1 | treat==4) & college==0, by(treat)ttest sup_sales if dem==1 & (treat==1 | treat==3) & college==0, by(treat)ttest sup_sales if dem==1 & (treat==1 | treat==2) & college==0, by(treat)*Support for Taxes by Level of Political Interest*These results are shown in Figure A8.*Republicans' support for the progressive income tax in the control/treatment groups ///*among those with high political interestsum sup_prog if dem==0 & treat==1 & polint==1sum sup_prog if dem==0 & treat==4 & polint==1sum sup_prog if dem==0 & treat==3 & polint==1sum sup_prog if dem==0 & treat==2 & polint==1*T-tests for these Republicans' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==0 & (treat==1 | treat==4) & polint==1, by(treat)ttest sup_prog if dem==0 & (treat==1 | treat==3) & polint==1, by(treat)ttest sup_prog if dem==0 & (treat==1 | treat==2) & polint==1, by(treat)*Republicans' support for the progressive income tax in the control/treatment groups ///*among those with low political interestsum sup_prog if dem==0 & treat==1 & polint==0sum sup_prog if dem==0 & treat==4 & polint==0sum sup_prog if dem==0 & treat==3 & polint==0sum sup_prog if dem==0 & treat==2 & polint==0*T-tests for these Republicans' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==0 & (treat==1 | treat==4) & polint==0, by(treat)ttest sup_prog if dem==0 & (treat==1 | treat==3) & polint==0, by(treat)ttest sup_prog if dem==0 & (treat==1 | treat==2) & polint==0, by(treat)*Democrats' support for the progressive income tax in the control/treatment groups ///*among those with high political interestsum sup_prog if dem==1 & treat==1 & polint==1sum sup_prog if dem==1 & treat==4 & polint==1sum sup_prog if dem==1 & treat==3 & polint==1sum sup_prog if dem==1 & treat==2 & polint==1*T-tests for these Democrats' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==1 & (treat==1 | treat==4) & polint==1, by(treat)ttest sup_prog if dem==1 & (treat==1 | treat==3) & polint==1, by(treat)ttest sup_prog if dem==1 & (treat==1 | treat==2) & polint==1, by(treat)*Democrats' support for the progressive income tax in the control/treatment groups ///*among those with low political interestsum sup_prog if dem==1 & treat==1 & polint==0sum sup_prog if dem==1 & treat==4 & polint==0sum sup_prog if dem==1 & treat==3 & polint==0sum sup_prog if dem==1 & treat==2 & polint==0*T-tests for these Democrats' support for the progressive income tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_prog if dem==1 & (treat==1 | treat==4) & polint==0, by(treat)ttest sup_prog if dem==1 & (treat==1 | treat==3) & polint==0, by(treat)ttest sup_prog if dem==1 & (treat==1 | treat==2) & polint==0, by(treat)*Republicans' support for the sales tax in the control/treatment groups ///*among those with high political interestsum sup_sales if dem==0 & treat==1 & polint==1sum sup_sales if dem==0 & treat==4 & polint==1sum sup_sales if dem==0 & treat==3 & polint==1sum sup_sales if dem==0 & treat==2 & polint==1*T-tests for these Republicans' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==0 & (treat==1 | treat==4) & polint==1, by(treat)ttest sup_sales if dem==0 & (treat==1 | treat==3) & polint==1, by(treat)ttest sup_sales if dem==0 & (treat==1 | treat==2) & polint==1, by(treat)*Republicans' support for the sales tax in the control/treatment groups ///*among those with low political interestsum sup_sales if dem==0 & treat==1 & polint==0sum sup_sales if dem==0 & treat==4 & polint==0sum sup_sales if dem==0 & treat==3 & polint==0sum sup_sales if dem==0 & treat==2 & polint==0*T-tests for these Republicans' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==0 & (treat==1 | treat==4) & polint==0, by(treat)ttest sup_sales if dem==0 & (treat==1 | treat==3) & polint==0, by(treat)ttest sup_sales if dem==0 & (treat==1 | treat==2) & polint==0, by(treat)*Democrats' support for the sales tax in the control/treatment groups ///*among those with high political interestsum sup_sales if dem==1 & treat==1 & polint==1sum sup_sales if dem==1 & treat==4 & polint==1sum sup_sales if dem==1 & treat==3 & polint==1sum sup_sales if dem==1 & treat==2 & polint==1*T-tests for these Democrats' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==1 & (treat==1 | treat==4) & polint==1, by(treat)ttest sup_sales if dem==1 & (treat==1 | treat==3) & polint==1, by(treat)ttest sup_sales if dem==1 & (treat==1 | treat==2) & polint==1, by(treat)*Democrats' support for the sales tax in the control/treatment groups ///*among those with low political interestsum sup_sales if dem==1 & treat==1 & polint==0sum sup_sales if dem==1 & treat==4 & polint==0sum sup_sales if dem==1 & treat==3 & polint==0sum sup_sales if dem==1 & treat==2 & polint==0*T-tests for these Democrats' support for the sales tax ///*Control versus party cues, inequality info, and party cues + inequality infottest sup_sales if dem==1 & (treat==1 | treat==4) & polint==0, by(treat)ttest sup_sales if dem==1 & (treat==1 | treat==3) & polint==0, by(treat)ttest sup_sales if dem==1 & (treat==1 | treat==2) & polint==0, by(treat)