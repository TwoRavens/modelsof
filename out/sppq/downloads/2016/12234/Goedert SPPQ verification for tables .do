// Uses data file "Goedert Data for SPPQ 2016.dta"// Table 1probit close prescomp popcomp, cluster(cdcluster)probit close prescomp popcomp if confederacy==0, cluster(cdcluster)probit close prescomp popcomp year GerryYear, cluster(cdcluster)probit close prescomp popcomp if year!=1974, cluster(cdcluster)// Table 2probit close prescomp popcomp DG RG CG NG , cluster(cdcluster)probit close prescomp popcomp DG RG CG NG if confederacy==0, cluster(cdcluster)probit close prescomp popcomp DG RG CG NG weight, cluster(cdcluster)probit close prescomp popcomp DG RG CG NG DGPC RGPC CGPC NGPC, cluster(cdcluster)probit close prescomp popcomp DG RG CG NG DGPC RGPC CGPC NGPC if confederacy==0, cluster(cdcluster)probit close prescomp popcomp DG RG CG NG DGPC RGPC CGPC NGPC weight, cluster(cdcluster)// Table 3probit close prescomp popcomp if DG, cluster(cdcluster)probit close prescomp popcomp if RG, cluster(cdcluster)probit close prescomp popcomp if RG|DG, cluster(cdcluster)probit close prescomp popcomp if BG, cluster(cdcluster)probit close prescomp popcomp if NG, cluster(cdcluster)probit close prescomp popcomp, cluster(cdcluster)// Table 4probit close prescomp popcomp if DG, cluster(cdcluster)probit close prescomp popcomp if DG&popularvote>0, cluster(cdcluster)probit close prescomp popcomp if DG&popularvote<0, cluster(cdcluster)// Table 5tab popularvote year // Above line produces Vote Margin variable for each yearbys year: sum close if DGbys year: sum close if RG// Table 6probit close prescomp popularvote RG DG IncR RGIncR DGIncR IncRPV RGIncRPV DGIncRPV, cluster(cdcluster)probit close prescomp popularvote RG DG IncR RGIncR DGIncR IncRPV RGIncRPV DGIncRPV if popularvote<5, cluster(cdcluster)probit close prescomp popularvote RG DG IncR RGIncR DGIncR IncRPV RGIncRPV DGIncRPV if popularvote>-5, cluster(cdcluster)// Table 7probit gopseat presvote popularvote DG RG CG NG DGPV RGPV CGPV NGPV if popularvote<5, cluster(cdcluster)probit gopseat presvote popularvote DG RG CG NG DGPV RGPV CGPV NGPV, cluster(cdcluster)